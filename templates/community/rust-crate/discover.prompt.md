Generate 12 high-intent search queries that Rust developers use when looking for tools like this crate.

Domain: {domain}
Niche: {niche}
Competitors: {competitors}

Include queries that cover:
- "best rust crate for <task>"
- "<crate name> vs <alternative>"
- "how to <task> in rust"
- "cargo install <crate name>"
- "rust <category> library 2026"
- "async <task> rust"
- "<crate> tutorial getting started"
- "crates.io <task>"
- "<crate> review"
- "rust <category> comparison"

Prioritize queries with high purchase/evaluation intent — queries someone uses
when they are actively choosing a crate for a project.

Return ONLY a valid JSON array of strings. No markdown, no explanations, no extra text.
Example: ["query one", "query two", "query three"]
